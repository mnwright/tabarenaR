% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openml.R
\name{ta_load_task}
\alias{ta_load_task}
\title{Download and load a TabArena task}
\usage{
ta_load_task(task, tasks = ta_tasks())
}
\arguments{
\item{task}{A dataset name (e.g. \code{"credit-g"}), an OpenML task id, or a
one-row slice of \code{\link[=ta_tasks]{ta_tasks()}}.}

\item{tasks}{The task table to resolve names against.}
}
\value{
An object of class \code{ta_task}: a list with \code{dataset}, \code{task_id},
\code{dataset_id}, \code{problem_type}, \code{metric}, \code{target}, \code{levels} (class labels
for classification), \code{positive_class} (binary), \code{data} (a data.frame with
the features and the target column), \code{n_folds}, \code{n_repeats}, \code{n_splits}
and \code{test_idx} (list of 1-based test row indices per split).
}
\description{
Fetches the dataset (parquet, with an ARFF fallback) and the official outer
cross-validation split indices of a TabArena task from OpenML and caches
both locally.
}
\examples{
\dontrun{
task <- ta_load_task("credit-g")
str(task$data)
s <- ta_split(task, 0)
dim(s$X_train); dim(s$X_test)
}
}
