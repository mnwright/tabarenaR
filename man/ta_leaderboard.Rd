% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leaderboard.R
\name{ta_leaderboard}
\alias{ta_leaderboard}
\title{Compute a TabArena leaderboard}
\usage{
ta_leaderboard(
  results = NULL,
  reference = "paper",
  tasks = NULL,
  splits = "evaluated",
  imputation = FALSE,
  calibration = "RF (default)",
  bootstrap_rounds = 100,
  seed = 0,
  include_systems = TRUE,
  methods = NULL,
  verbose = TRUE
)
}
\arguments{
\item{results}{Results of the new method(s) as returned by
\code{\link[=ta_evaluate]{ta_evaluate()}} (or any data.frame with \code{method}, \code{dataset}, \code{fold},
\code{metric_error}, and ideally \code{time_train_s}, \code{time_infer_s}). \code{NULL}
reproduces the leaderboard of the reference methods alone.}

\item{reference}{The reference results: \code{"paper"} (bundled snapshot of the
TabArena paper results, 44 methods), \code{"live"} (downloads the current
per-method results behind the online leaderboard, see \code{\link[=ta_results]{ta_results()}}),
or a results data.frame.}

\item{tasks}{Which datasets form the field: \code{NULL} (all 51), a subset name
accepted by \code{\link[=ta_tasks]{ta_tasks()}}, a character vector of dataset names, or a task
table.}

\item{splits}{Which splits form the field: \code{"evaluated"} (default; the
(dataset, split) pairs the new results cover, so that a TabArena-Lite run
is compared on TabArena-Lite), \code{"all"}, \code{"lite"} or integer split
indices.}

\item{imputation}{If \code{FALSE} (default; the website's "no imputation"
board) methods lacking a result on any task of the field are dropped. If
\code{TRUE}, their missing tasks are filled with the result of the
\code{calibration} method (a default random forest) and flagged as imputed,
like the website's imputation toggle.}

\item{calibration}{The method pinned to Elo 1000 and used to fill missing
results; \code{"RF (default)"} on the official leaderboard.}

\item{bootstrap_rounds, seed}{Bootstrap settings, see \code{\link[=ta_elo]{ta_elo()}}.}

\item{include_systems}{Keep whole-pipeline systems such as AutoGluon in the
field (\code{TRUE}), or compare against individual models only (\code{FALSE}, the
website's default pool). Requires \code{method_class} information, which the
live results carry.}

\item{methods}{Optional character vector restricting the reference methods.}

\item{verbose}{Print progress.}
}
\value{
A data.frame of class \code{ta_leaderboard}, one row per method sorted
by Elo, with attributes \code{"per_task"} (the per-(method, dataset, split)
table with rank, improvability etc.), \code{"elo"} (the \code{\link[=ta_elo]{ta_elo()}} output),
\code{"dropped"} (methods removed for missing results) and \code{"field"} (the
(dataset, split) pairs used).
}
\description{
Scores one or more new methods against the published results of the
leaderboard methods and returns the leaderboard columns of the official
website: Elo (with 95\% interval), normalized score, mean rank, harmonic
rank, improvability, win rate, median train / predict time per 1000 rows
and the share of imputed tasks.
}
\seealso{
\code{\link[=ta_benchmark]{ta_benchmark()}} to evaluate and rank in one call.
}
