% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{ta_benchmark}
\alias{ta_benchmark}
\title{Evaluate a learner on TabArena and get its Elo in one call}
\usage{
ta_benchmark(
  learner,
  tasks = ta_tasks(),
  splits = "lite",
  reference = "paper",
  imputation = FALSE,
  calibration = "RF (default)",
  bootstrap_rounds = 100,
  seed = 0,
  include_systems = TRUE,
  results_dir = NULL,
  stop_on_error = FALSE
)
}
\arguments{
\item{learner}{A \code{\link[=ta_learner]{ta_learner()}}.}

\item{tasks}{A task table from \code{\link[=ta_tasks]{ta_tasks()}} (possibly subset).}

\item{splits}{\code{"lite"} (split 0 of every dataset, i.e. TabArena-Lite),
\code{"all"} (every split; 9 or 30 per dataset), \code{"first_repeat"} (splits 0 to
2) or an integer vector of split indices.}

\item{reference}{The reference results: \code{"paper"} (bundled snapshot of the
TabArena paper results, 44 methods), \code{"live"} (downloads the current
per-method results behind the online leaderboard, see \code{\link[=ta_results]{ta_results()}}),
or a results data.frame.}

\item{imputation}{If \code{FALSE} (default; the website's "no imputation"
board) methods lacking a result on any task of the field are dropped. If
\code{TRUE}, their missing tasks are filled with the result of the
\code{calibration} method (a default random forest) and flagged as imputed,
like the website's imputation toggle.}

\item{calibration}{The method pinned to Elo 1000 and used to fill missing
results; \code{"RF (default)"} on the official leaderboard.}

\item{bootstrap_rounds}{Number of dataset-level bootstrap rounds for the
Elo confidence interval (see \code{\link[=ta_elo]{ta_elo()}}).}

\item{seed}{Seed used both for the learner (see \code{\link[=ta_evaluate]{ta_evaluate()}}) and the
bootstrap.}

\item{include_systems}{Keep whole-pipeline systems such as AutoGluon in the
field (\code{TRUE}), or compare against individual models only (\code{FALSE}, the
website's default pool). Requires \code{method_class} information, which the
live results carry.}

\item{results_dir}{Optional directory where every finished (dataset, split)
result is checkpointed as an \code{.rds} file, so an interrupted run can be
resumed by calling the function again.}

\item{stop_on_error}{If \code{FALSE} (default), a failing split records \code{NA}
as its error (and a warning) instead of aborting the whole run. Such
tasks count as missing / imputed on the leaderboard.}
}
\value{
A list with \code{results} (the per-split results), \code{leaderboard} (the
full leaderboard) and \code{summary} (the new method's leaderboard row).
}
\description{
Runs \code{\link[=ta_evaluate]{ta_evaluate()}} and then \code{\link[=ta_leaderboard]{ta_leaderboard()}}.
}
\examples{
\dontrun{
lm_learner <- ta_learner(
  name = "lm / glm (R)",
  train = function(X, y, ctx) {
    d <- cbind(X, .y = y)
    if (ctx$problem_type == "regression") lm(.y ~ ., d)
    else if (ctx$problem_type == "binary") glm(.y ~ ., d, family = binomial())
    else nnet::multinom(.y ~ ., d, trace = FALSE)
  },
  predict = function(model, X, ctx) {
    if (ctx$problem_type == "regression") predict(model, X)
    else if (ctx$problem_type == "binary") predict(model, X, type = "response")
    else predict(model, X, type = "probs")
  },
  preprocess = function(X_train, X_test, ctx) ta_prep_basic(X_train, X_test, one_hot = TRUE)
)
out <- ta_benchmark(lm_learner, tasks = ta_tasks("tiny"), splits = "lite")
out$summary
}
}
