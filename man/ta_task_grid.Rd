% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{ta_task_grid}
\alias{ta_task_grid}
\title{The (dataset, split) grid of the benchmark}
\usage{
ta_task_grid(tasks = ta_tasks(), splits = "all")
}
\arguments{
\item{tasks}{A task table from \code{\link[=ta_tasks]{ta_tasks()}}.}

\item{splits}{Which splits to keep: \code{"all"}, \code{"lite"} (split 0 only, i.e.
TabArena-Lite), or an integer vector of split indices.}
}
\value{
A data.frame with columns \code{dataset}, \code{task_id}, \code{split}, \code{repeat},
\code{fold}, \code{problem_type}, \code{metric}, \code{n_train}, \code{n_test}.
}
\description{
Expands the task table into one row per outer split. The \code{split} index is
what the official results files call \code{fold}: \code{split = n_folds * repeat + fold} (0-based).
}
