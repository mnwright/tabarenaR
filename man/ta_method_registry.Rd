% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{ta_method_registry}
\alias{ta_method_registry}
\title{Registry of methods with published TabArena results}
\usage{
ta_method_registry(refresh = FALSE)
}
\arguments{
\item{refresh}{If \code{TRUE}, rebuild the registry from GitHub first (see
\code{\link[=ta_registry_refresh]{ta_registry_refresh()}}); if \code{FALSE} use the refreshed copy in the cache
when there is one, else the bundled snapshot.}
}
\value{
A data.frame with columns \code{variable} (Python variable name),
\code{method}, \code{suite}, \code{method_type}, \code{method_class}, \code{cache_type},
\code{display_name}, \code{verified}, \code{commercial_use}, \code{source_file} and
\code{current} (\code{TRUE} for the methods in the current leaderboard
collection).
}
\description{
Every method benchmarked on TabArena is declared in the \code{tabarena} Python
package with a \code{method} name and a \code{suite} (the run it belongs to); the two
locate its results file in TabArena's public storage. The registry is a
table of these declarations. A snapshot is bundled with the package;
\code{\link[=ta_registry_refresh]{ta_registry_refresh()}} rebuilds it from the current source tree of the
\code{autogluon/tabarena} GitHub repository.
}
