% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{ta_tasks}
\alias{ta_tasks}
\title{The TabArena-v0.1 task suite}
\usage{
ta_tasks(subset = NULL)
}
\arguments{
\item{subset}{Optional name of a dataset-level subset, or a character vector
of dataset names / integer OpenML task ids to keep. Named subsets follow
the official leaderboard: \code{"all"}, \code{"binary"}, \code{"multiclass"},
\code{"classification"}, \code{"regression"}, \code{"tiny"} (at most 2000 training
rows), \code{"small"} (at most 10000), \code{"2k-10k"}, \code{"medium"} (10001 to
100000), \code{"tabpfn"} (datasets TabPFNv2 can run on), \code{"tabicl"},
\code{"numerical"}, \code{"low_cats"}, \code{"high_cats"}, \code{"low_features"},
\code{"high_features"}.}
}
\value{
A data.frame with columns \code{dataset}, \code{task_id}, \code{dataset_id},
\code{target}, \code{problem_type}, \code{metric}, \code{n_folds}, \code{n_repeats}, \code{n_splits},
\code{n_instances}, \code{n_features}, \code{n_classes}, \code{n_train}, \code{n_test},
\code{pct_categorical}, \code{can_run_tabpfnv2}, \code{can_run_tabicl}, \code{domain},
\code{source}, \code{licence}.
}
\description{
Returns the curated task table shipped with the package: one row per
dataset with its OpenML task id, target column, problem type, official
metric and the number of outer cross-validation splits (\code{n_folds = 3}
folds times \code{n_repeats} repeats; 10 repeats for datasets with fewer than
2500 rows, 3 otherwise).
}
\examples{
head(ta_tasks())
ta_tasks("regression")$dataset
}
