% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{ta_results_live}
\alias{ta_results_live}
\title{Download the live per-method results behind the online leaderboard}
\usage{
ta_results_live(
  registry = ta_method_registry(),
  current_only = TRUE,
  use_display_names = TRUE,
  refresh = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{registry}{A registry data.frame from \code{\link[=ta_method_registry]{ta_method_registry()}}.}

\item{current_only}{Keep only the methods on the current leaderboard
(\code{current == TRUE} in the registry), dropping superseded runs.}

\item{use_display_names}{Rename the methods as the website does, i.e.
\code{"<display name> (default|tuned|tuned + ensemble)"} (for example
\code{"RandomForest (default)"} instead of the internal \code{"RF (default)"}).
The original identifier is kept in column \code{method_id}.}

\item{refresh}{Re-download files already in the cache.}

\item{verbose}{Print progress.}
}
\value{
A results data.frame (see \code{\link[=ta_results]{ta_results()}}) with the extra columns
\code{method_id}, \code{display_name}, \code{verified} and \code{commercial_use}.
}
\description{
TabArena publishes one result file per benchmarked method (in a public
Cloudflare R2 bucket or the legacy public S3 bucket). This function reads
the method registry (see \code{\link[=ta_method_registry]{ta_method_registry()}}), downloads every method's
results file, and combines them.
}
