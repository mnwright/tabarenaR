% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{ta_metric_error}
\alias{ta_metric_error}
\title{Official TabArena metric error}
\usage{
ta_metric_error(y_true, pred, problem_type, levels = NULL, metric = NULL)
}
\arguments{
\item{y_true}{Observed targets: a factor (classification) or numeric vector
(regression).}

\item{pred}{Predictions. Regression: a numeric vector. Binary: a numeric
vector with the probability of the positive class (the second factor
level), or a matrix/data.frame of class probabilities with one column per
class. Multiclass: a matrix/data.frame of class probabilities with one
column per class (columns are matched by name when named, otherwise they
are assumed to follow \code{levels}).}

\item{problem_type}{One of \code{"binary"}, \code{"multiclass"}, \code{"regression"}.}

\item{levels}{Class labels (defaults to \code{levels(y_true)}).}

\item{metric}{Optional metric name; defaults to the official metric of the
problem type (\code{"roc_auc"}, \code{"log_loss"}, \code{"rmse"}).}
}
\value{
A single non-negative number.
}
\description{
Computes the \emph{error} (lower is better) TabArena scores a prediction with:
\verb{1 - ROC AUC} for binary classification, log loss for multiclass
classification and RMSE for regression.
}
\examples{
y <- factor(c("a", "b", "b", "a"))
ta_metric_error(y, c(0.1, 0.9, 0.6, 0.4), "binary")
}
