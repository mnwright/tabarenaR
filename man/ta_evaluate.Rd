% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{ta_evaluate}
\alias{ta_evaluate}
\title{Run a learner on TabArena tasks}
\usage{
ta_evaluate(
  learner,
  tasks = ta_tasks(),
  splits = "lite",
  results_dir = NULL,
  stop_on_error = FALSE,
  seed = 0L
)
}
\arguments{
\item{learner}{A \code{\link[=ta_learner]{ta_learner()}}.}

\item{tasks}{A task table from \code{\link[=ta_tasks]{ta_tasks()}} (possibly subset).}

\item{splits}{\code{"lite"} (split 0 of every dataset, i.e. TabArena-Lite),
\code{"all"} (every split; 9 or 30 per dataset), \code{"first_repeat"} (splits 0 to
2) or an integer vector of split indices.}

\item{results_dir}{Optional directory where every finished (dataset, split)
result is checkpointed as an \code{.rds} file, so an interrupted run can be
resumed by calling the function again.}

\item{stop_on_error}{If \code{FALSE} (default), a failing split records \code{NA}
as its error (and a warning) instead of aborting the whole run. Such
tasks count as missing / imputed on the leaderboard.}

\item{seed}{Seed set before every split for reproducibility of the learner.}
}
\value{
A data.frame of per-split results with the columns used by the
official results files: \code{method}, \code{dataset}, \code{fold} (the split index),
\code{metric_error}, \code{time_train_s}, \code{time_infer_s}, \code{metric},
\code{problem_type}, plus \code{task_id}, \code{repeat}, \code{fold_in_repeat} and \code{error_message}.
}
\description{
Evaluates a \code{\link[=ta_learner]{ta_learner()}} on the requested tasks and outer splits with the
official protocol: for every split the learner is trained on the training
rows, predicts the test rows, and the prediction is scored with the task's
metric. Training and prediction wall-clock times are recorded.
}
