% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learner.R
\name{ta_prep_basic}
\alias{ta_prep_basic}
\title{Basic preprocessing for learners that cannot handle missing values}
\usage{
ta_prep_basic(X_train, X_test, ctx = NULL, one_hot = FALSE, max_levels = 100L)
}
\arguments{
\item{X_train, X_test}{Feature data.frames.}

\item{ctx}{The split context (unused, accepted for compatibility with
\code{\link[=ta_learner]{ta_learner()}}'s \code{preprocess} slot).}

\item{one_hot}{If \code{TRUE}, factors are expanded into 0/1 indicator columns so
the result is purely numeric.}

\item{max_levels}{Factors with more levels than this are dropped when
\code{one_hot = TRUE} (to avoid gigantic design matrices).}
}
\value{
\code{list(X_train = , X_test = )}.
}
\description{
Median-imputes numeric columns and adds an explicit \code{"(missing)"} level to
factors, using training statistics only. Character and logical columns are
converted to factors, factor levels are aligned between train and test, and
constant columns are dropped. Optionally one-hot encodes factors.
}
