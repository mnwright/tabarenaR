% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learner.R
\name{ta_learner}
\alias{ta_learner}
\title{Wrap a train/predict pair as a TabArena learner}
\usage{
ta_learner(train, predict, name = "my_method", preprocess = NULL)
}
\arguments{
\item{train}{A function \verb{function(X, y, ctx)}.}

\item{predict}{A function \verb{function(model, X, ctx)}.}

\item{name}{The method name shown on the leaderboard.}

\item{preprocess}{Optional function \verb{function(X_train, X_test, ctx)}
returning \code{list(X_train = , X_test = )}, applied before \code{train} and
\code{predict}. See \code{\link[=ta_prep_basic]{ta_prep_basic()}} for a ready-made one.}
}
\value{
An object of class \code{ta_learner}.
}
\description{
Any R model can be benchmarked by supplying two functions.
}
\details{
\code{train(X, y, ctx)} receives the training features \code{X} (a data.frame with
numeric and factor columns, possibly containing \code{NA}), the training target
\code{y} (a factor for classification, numeric for regression) and a context
list \code{ctx} with \code{dataset}, \code{task_id}, \code{split}, \code{repeat}, \code{fold},
\code{problem_type}, \code{metric}, \code{target}, \code{levels} and \code{positive_class}. It must
return a fitted model object.

\code{predict(model, X, ctx)} receives that model and the test features and must
return, depending on \code{ctx$problem_type}:
\itemize{
\item \code{"regression"}: a numeric vector of predictions;
\item \code{"binary"}: a numeric vector with the probability of \code{ctx$positive_class}
(the second level of \code{ctx$levels}), or a probability matrix with one
column per class;
\item \code{"multiclass"}: a matrix (or data.frame) of class probabilities with one
column per class, ideally with columns named after \code{ctx$levels}.
}
}
\examples{
\dontrun{
rf <- ta_learner(
  name = "ranger (R)",
  train = function(X, y, ctx) {
    ranger::ranger(x = X, y = y, probability = ctx$problem_type != "regression", num.threads = 4)
  },
  predict = function(model, X, ctx) {
    p <- predict(model, data = X, num.threads = 4)$predictions
    if (ctx$problem_type == "regression") p else p  # matrix of class probabilities
  },
  preprocess = ta_prep_basic
)
res <- ta_evaluate(rf, tasks = ta_tasks("tiny"), splits = "lite")
ta_leaderboard(res)
}
}
